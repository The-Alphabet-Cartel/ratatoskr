# ============================================================
# Ratatoskr — Environment Configuration Template
# Copy to .env and fill in values before running.
# Sensitive values (tokens, passwords) go in Docker Secrets,
# NOT here. See docker-compose.yml for secret definitions.
# ============================================================
# --- Runtime ---
PUID=1000                        # User ID to run the bot as
PGID=1000                        # Group ID to run the bot as

# --- Bot Behavior ---
COMMAND_PREFIX=!
GUILD_ID=                 # The Alphabet Cartel Fluxer guild ID

# --- Logging ---
LOG_LEVEL=INFO            # DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_FORMAT=human          # human (colorized) | json (structured)
LOG_CONSOLE=true          # Log to console
RATATOSKR_LOG_FILE=/app/logs/ratatoskr.log

# --- Bot-Specific Settings ---
# TODO: Add your bot's specific env vars here, following the naming
#       convention: {BOT_PREFIX}_{CATEGORY}_{NAME}
#       Example: PORTIA_LOBBY_CHANNEL_ID=
