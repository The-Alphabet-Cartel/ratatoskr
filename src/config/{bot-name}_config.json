{
  "_metadata": {
    "file_version": "v1.0.0",
    "last_modified": "2026-02-27",
    "bot": "ratatoskr",
    "clean_architecture": "Compliant"
  },

  "logging": {
    "description": "Logging configuration — shared pattern across all Bragi bots",
    "level": "INFO",
    "format": "human",
    "file": "/app/logs/ratatoskr.log",
    "console": true,
    "validation": {
      "level": {
        "type": "string",
        "allowed_values": ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"],
        "required": true
      },
      "format": {
        "type": "string",
        "allowed_values": ["human", "json"],
        "required": true
      }
    }
  },

  "bot": {
    "description": "Core bot settings — shared pattern across all Bragi bots",
    "command_prefix": "!",
    "guild_id": "",
    "validation": {
      "command_prefix": {
        "type": "string",
        "required": false
      },
      "guild_id": {
        "type": "string",
        "required": false
      }
    }
  }
}